// Title: Lexicographically Smallest Generated String
            // Difficulty: Hard
            // Language: C++
            // Link: https://leetcode.com/problems/lexicographically-smallest-generated-string/


                                if(str1[k]=='T')

                                    if(string(w.
                                        
                                        break;
                                    }
                                }
                            }

                            if(valid && string
                                fixed=true;
                                break;
                            }
                        }

                        if(fixed) break;

                        w[pos]=original;
                    }

                    if(!fixed) return "";
                }
            }
        }

        return string(w.begin(),w.end());
{
begin()+k,w.begin()+k+m)!=str2){
valid=false;
(w.begin()+i,w.begin()+i+m)!=str2){
    }
};
